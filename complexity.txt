Во всех языках реализация reverse_list выполняет за O(N) по времени и памяти (ибо создаётся новый список).

Функция sums_to в языках кроме C оптимизируется хэш-таблицей, а посему работает за O(N), если считать разрядность чисел константой.
